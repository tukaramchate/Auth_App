server:
  port: 8083

#Database Connectivity
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/auth_app
    username: root
    password: math#1206*B

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
    show-sql: true

# jpa and hibernate configurations